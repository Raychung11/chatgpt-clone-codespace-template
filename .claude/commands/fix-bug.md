Fix a bug in the VideoSaaS platform.

Steps:
1. Read the file(s) mentioned — never guess, always read first
2. Identify the root cause before changing anything
3. Make the minimal targeted fix — don't refactor surrounding code
4. Check for the same bug pattern in related files
5. Commit the fix and push to branch `claude/ai-video-saas-platform-bX7QE`

The bug to fix: $ARGUMENTS
